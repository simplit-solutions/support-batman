# ========================================
# TIPO DE DASHBOARD
# ========================================
# Elige: "kubernetes" o "database"
dashboard_type = "kubernetes"

# ========================================
# PROYECTO DE MONITOREO CENTRAL
# ========================================
# Este es el proyecto donde se crearán los dashboards y alertas
monitoring_project_id = "monitoring-central-project"
monitoring_region     = "us-central1"

# ========================================
# VARIABLES PARA KUBERNETES DASHBOARD
# ========================================
# Solo necesarias si dashboard_type = "kubernetes"
target_project_id = "production-k8s-project"
cluster_name      = "my-production-cluster"

# ========================================
# VARIABLES PARA DATABASE DASHBOARD
# ========================================
# Solo necesarias si dashboard_type = "database"
database_project_id = "production-db-project"
database_instance   = "my-cloudsql-instance"
database_name       = "my_database"

# ========================================
# ALERTAS
# ========================================
enable_alerts = true

# Notificaciones por Email
notification_emails = [
  "ops-team@company.com",
  "devops-lead@company.com"
]

# Notificaciones por Slack (opcional)
# Para obtener el auth_token:
# 1. Autoriza Slack como Notification Channel en Cloud Monitoring (Console), o
# 2. Crea una Slack App y usa el Bot User OAuth token.
slack_auth_token  = null # Reemplazar con tu auth_token de Slack
slack_channel_name = "#alerts"

# ========================================
# FILTRADO DE NAMESPACES (KUBERNETES SOLO)
# ========================================
# Modo de filtrado: "none" (todos) o "exact" (específicos)
namespace_filter = "none"

# Namespaces específicos (ignorado si namespace_filter = "none")
namespace_list = []

# Opcional: Agrupar namespaces en alertas por categoría
# Solo usado si namespace_filter = "exact"
namespace_groups = {
  # "production" = ["namespace1", "namespace2"]
  # "staging"    = ["staging-ns1", "staging-ns2"]
}
